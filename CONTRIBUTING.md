# Contributing to Esports Tycoon

## Development setup
1. Clone the repo and install: `pip install -e .[dev]`
2. Run tests: `pytest`

## Coding standards
- Keep diffs small and focused.
- Add tests for new behavior.
- Match the surrounding style.

## Submitting changes
Open a PR against `main` with a clear description.
