# Added by CompanyOS so build artifacts/bytecode are never committed.
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.venv/
venv/
node_modules/
dist/
build/
.DS_Store
*.log
.env
.env.*
!.env.example

# Per-slice recap artifacts (runs/<slice_id>/recap.md + feed.snapshot.html) are
# regenerated on every play; they are screenshot/share outputs, not source.
runs/
